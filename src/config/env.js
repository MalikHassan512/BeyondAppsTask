import {API_BASE_URL, API_KEY, LOCATION_API } from '@env'; // Importing environment configuration

// Exporting the environment configuration
export default {
    API_BASE_URL,
    API_KEY,
    LOCATION_API
    };